{
  "name": "com.eclipsesource.tabris.launchmonitor.demoapp",
  "version": "1.0.0",
  "main": "dist/app.js",
  "private": true,
  "scripts": {
    "test": "npm run build && npm run lint",
    "lint": "tslint --project . 'src/**/*.ts' 'src/**/*.tsx'",
    "build": "tsc -p .",
    "watch": "tsc -w -p . --preserveWatchOutput",
    "start": "tabris serve -wia",
    "serve": "tabris serve"
  },
  "dependencies": {
    "tabris": "~3.4.0"
  },
  "devDependencies": {
    "tabris-plugin-launchmonitor": "file:..",
    "tslint": "^5.11.0",
    "typescript": "~3.3.4000"
  }
}
